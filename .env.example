# Secret de signature des sessions Payload.
# En générer un avec :  node -e "console.log(crypto.randomUUID())"
PAYLOAD_SECRET=

# Neon Postgres. Prendre la chaîne du pooler (hôte en `-pooler`, port 5432),
# pas la connexion directe qui n'est joignable qu'en IPv6.
# Remplacer `sslmode=require` par `sslmode=verify-full` dans la chaîne fournie
# par Neon : le pilote Postgres traite déjà `require` comme `verify-full`, mais
# l'annonce par un avertissement à chaque connexion (et à chaque build).
DATABASE_URL=

# UploadThing : jeton de l'application, onglet "API Keys" du dashboard.
UPLOADTHING_TOKEN=

# Resend : envoi du formulaire de contact.
RESEND_API_KEY=
# Boîte qui reçoit les messages. Tant qu'aucun domaine n'est vérifié chez
# Resend, ce doit être l'adresse d'inscription du compte.
CONTACT_TO_EMAIL=
# Expéditeur, une fois un domaine vérifié. Sinon onboarding@resend.dev.
# CONTACT_FROM_EMAIL="BAPZ Studio <contact@exemple.fr>"

# Cloudflare Turnstile : vérification anti-robot des formulaires.
# Clés d'un widget créé dans le tableau de bord Cloudflare (Turnstile), avec
# le domaine du site. En local, les clés de test de Cloudflare laissent tout
# passer : site 1x00000000000000000000AA, secret
# 1x0000000000000000000000000000000AA. Sans clé secrète, la vérification est
# ignorée en développement et TOUT ENVOI EST REFUSÉ en production.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Adresse publique du site, sans barre oblique finale (URL canoniques, sitemap,
# cartes de partage). Vide en local : le repli localhost suffit. OBLIGATOIRE en
# production, où le build échoue si elle est vide — sans quoi le site partirait
# avec des URL canoniques en localhost, sans que rien ne le signale.
NEXT_PUBLIC_SITE_URL=
